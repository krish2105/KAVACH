# Node
node_modules/
.next/
out/
*.tsbuildinfo
npm-debug.log*

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/

# KAVACH runtime state — never commit action logs, they contain
# a record of everything the agent touched on this machine (§7)
logs/
*.jsonl
.kavach/

# Models (large, downloaded not committed) — Whisper/Kokoro, Phase 2
models/
*.onnx
*.gguf
*.bin

# Secrets — Picovoice AccessKey, 21st MCP key, Anthropic key
.env
.env.*
!.env.example

# macOS
.DS_Store

# Wake-word training corpus + checkpoints (multi-GB, regenerable from the YAML)
brain/wakeword/data/
brain/wakeword/output/
